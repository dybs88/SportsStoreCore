@model IdentityUser

@{
    ViewData["Title"] = "Edycja użytkownika";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Edytuj użytkownika</h2>
<form class="" asp-action="EditUser" method="post">
    <div class="form-group">
        <label asp-for="UserName">Nazwa użytkownika</label>
        <input class="form-control" type="text" asp-for="UserName" />
    </div>
    <div class="form-group">
        <label asp-for="Email">E-mail</label>
        <input class="form-control" type="text" asp-for="Email" />
    </div>
    <div>
        Nadawanie ról
        @await  Component.InvokeAsync("IdentityRolesTable", new { user = Model })
    </div>
    <button class="btn btn-success" type="submit">Zapisz</button>
    <a class="btn btn-secondary" asp-action="List" asp-controller="User">Anuluj</a>
</form>

